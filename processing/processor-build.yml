
steps: 
  - task: Bash@3
    displayName: Validate & Publish Processor
    inputs:
        targetType: inline
        script: docker build --build-arg databrickstoken=$(databrickstoken) --build-arg databrickshost=$(databrickshost) . --file="./dockerfile"
        workingDirectory: $(Build.SourcesDirectory)/processing

