
steps: 
  - task: Bash@3
    displayName: Build Ingress Container
    inputs:
        targetType: inline
        script: docker build -t ingress:$(Build.BuildId) --build-arg ingressconnectionstring=$(ingressconnectionstring) . -f ./dockerfile
        workingDirectory: $(Build.SourcesDirectory)/ingress
